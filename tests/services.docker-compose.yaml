# Keep in sync with .gitlab-ci.yml python-tests job services
networks:
  dlm_network:
    name: dlm_network
    # external: true

volumes:
  shared-tmpfs:
    name: shared-tmpfs
    driver: local
    driver_opts:
      type: "tmpfs"
      device: "tmpfs"
      o: "mode=1777,uid=1000,gid=1000"
